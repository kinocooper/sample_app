<h1>投稿一覧</h1>

<%  @lists.each do |list| %>

  <p>タイトル</p>
  <span>
    <%= link_to list.title, list_path(list.id) %>
<!--繰り返し中　レコードのタイトルを表示、リンク先のURLは現在のidを変数展開で作る-->
  </span>
  
  <p>画像</p>
  <%= image_tag list.iamge, size:"200x200" %>

<% end %>